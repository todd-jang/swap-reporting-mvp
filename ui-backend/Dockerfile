resources:
  limits:
    nvidia.com/gpu: 1 # 컨테이너가 사용할 GPU 개수 요청 (예: 1개)
  requests: # 리소스 요청은 선택 사항이지만, 스케줄링 정확도를 위해 권장
    nvidia.com/gpu: 1
    cpu: "4" # 필요한 CPU 코어 수
    memory: "16Gi" # 필요한 메모리 용량
